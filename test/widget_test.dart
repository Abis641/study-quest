// test/widget_test.dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  // Basic smoke test — expand with real widget tests once Firebase is configured
  test('placeholder test', () {
    expect(1 + 1, equals(2));
  });
}
